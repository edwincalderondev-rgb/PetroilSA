# PetroilSA — guía para trabajar en este repo

Ver también [README.md](README.md) para la estructura de carpetas de `assets/`.

## Identidad visual oficial (marca)

**Tipografía principal:** Poppins (cargada vía Google Fonts en `assets/css/base.css`, pesos 300–800).

**Colores corporativos del logotipo:**
- En positivo: azul claro `#3499CC` · azul oscuro `#080C41`
- En negativo (sobre fondos oscuros): blanco `#FBFEFF` · gris `#BEBEBE`

**Secundarios:**
- Azul petróleo `#154772`
- Verde institucional `#1D5949`
- Verde lima energético `#C7F547`
- Azul cielo `#359ACC`
- Blanco puro `#FFFFFF`

**Migración de paleta (2026-09-14):** todo el sitio usa ya SOLO estos 8 colores. `assets/css/base.css` tiene los 8 como `--brand-*` (única fuente de verdad — `--brand-navy`, `--brand-blue`, `--brand-petrol`, `--brand-green`, `--brand-lime`, `--brand-sky`, `--brand-white`, `--brand-gray`). Los nombres históricos que ya usan cientos de reglas en todo el proyecto (`--navy`, `--navy-deep`, `--teal`, `--teal-light`, `--amber`, `--mist`, y también `--route-*`) siguen existiendo tal cual — **no los renombres, cascan solos** — pero ahora son alias `var(--brand-*)` en vez de colores inventados. Antes de esta migración: `--navy` era `#0A2A43`, `--teal` era un turquesa `#0F8C82` y `--amber` un naranja `#E8871E`, ninguno de los 3 en el manual — por eso index/productos/quienes-somos se sentían "de sitios distintos" al navegar entre ellos. `--steel`/`--ink`/`--line`/`--white` NO se tocaron: son neutros de UI (texto, bordes), no decisiones de marca.

Excepción deliberada: **`--warn:#E8871E`** (mismo naranja de antes) se reservó para avisos/precauciones/ejemplos — cajas de seguridad en fichas técnicas, el badge "Dato de ejemplo", banners "página de ejemplo", disclaimers del chat. Ninguno de los 8 colores oficiales lee como "advertencia", así que ahí sí se mantiene un color fuera de manual a propósito. Si agregas un aviso/precaución nuevo, usa `var(--warn)`, no `var(--amber)` (ese ya es verde lima, de marca/CTA).

`portal-clientes/portal-clientes.html` quedó **fuera** de esta migración a propósito: es una página autocontenida que ni siquiera carga `base.css` (tiene su propio `<style>`/`:root`) — un prototipo de dashboard con lenguaje visual propio, no parte de la navegación principal.

## Patrones a respetar

- **Nunca pases una imagen de fondo con un `url()` relativo por una custom property** (`style="--hero-img:url('assets/img/x.jpg')"` + `background-image:var(--hero-img)` en un CSS). El `url()` se resuelve respecto a la hoja CSS (`assets/css/`), no al HTML, así que la foto da 404 y solo se ve el color de fondo. Ya no queda ningún caso de esto en el sitio (arreglado en `article.css` + 7 páginas — ver abajo) — si vuelves a necesitar una foto de fondo, usa el patrón `.page-hero-bg` (ver siguiente punto) o `.pb-bg` de `productos.css`: un `<img>` real con `object-fit:cover` y el degradado en un `::after`, nunca una custom property con `url()`.
- Para comprobar cómo se ve algo de verdad, renderiza con Edge headless (`msedge --headless=new --user-data-dir=<carpeta propia> --window-size=1440,4400 --screenshot=out.png file:///...`) y mira la imagen. El `--user-data-dir` propio es necesario si Edge ya está abierto.
  - `--screenshot` captura SOLO el viewport, no la página entera, y un `#ancla` en la URL no siempre desplaza (el JS de la página se lo come). Para mirar el pie, arma un HTML de usar y tirar en la raíz del repo con `base.css` + `footer.css` y el `<footer>` pegado tal cual — así las rutas relativas siguen resolviendo — y bórralo después.
  - **Edge headless no baja de ~500 px de ancho de layout** por más que pidas `--window-size=390,…`: la foto sale de 390 px pero maquetada a 500, así que el contenido aparece cortado por la derecha. Eso NO es un desbordamiento real en móvil; no lo "arregles" con CSS. Si necesitas ancho de celular de verdad, hay que emular el dispositivo por CDP, no por `--window-size`.
- **Si automatizas ediciones con PowerShell, guarda el `.ps1` con BOM UTF-8.** PowerShell 5.1 lee un `.ps1` sin BOM como ANSI: cada `é`/`¿` del script se reinterpreta y se reescribe doble-codificado (`¿Quiénes somos?` → `Â¿QuiÃ©nes somos?`) en las 27 páginas de golpe. Se detecta con `grep -rlP '\xc3\x83|\xc3\x82'`. Los `.ps1` del repo van con BOM; `sed`/`perl` desde Bash no tienen este problema porque no reinterpretan bytes.
  - Y al reemplazar bloques con regex, reemplaza **contenedores completos** (de `<div class="x">` hasta un lookahead del div siguiente) en vez de "desde la apertura hasta el primer `</div>`": los here-strings de PowerShell NO incluyen el salto de línea final, así que una plantilla sin `\r\n` explícito deja la etiqueta siguiente pegada y, si el script corre dos veces, se come un cierre o duplica el bloque. Pasó con `.footer-links-col` y `.container.footer-bottom`.

- **`.page-hero`** (fondo oscuro con imagen/gradiente) se define en `assets/css/article.css`. Cualquier página que use `class="page-hero"` (o una variante como `.pds-hero`/`.qs-hero`) DEBE cargar `article.css`, o el hero pierde el fondo oscuro y el texto queda en el color de tinta por defecto (ilegible). Ya se corrigió este olvido en las 15 páginas de `fichas-tecnicas/` y en `nosotros/quienes-somos.html` — no lo vuelvas a omitir en páginas nuevas.
  - El degradado vive en la custom property `--ph-gradient`, declarada en **`:root`** (`article.css`) desde el 2026-09-18 — antes estaba dentro de `.page-hero`. Se subió para que cualquier panel oscuro pueda reutilizar el mismo degradado sin volver a escribir los `rgba()` a mano (lo usan los `.ra-criteria`/`.ra-cert` de responsabilidad ambiental). Es semitransparente, porque está pensado para ir sobre la foto del hero: fuera de `.page-hero` hay que acompañarlo de un `background-color` opaco o el bloque sale lavado. Páginas SIN foto (fichas técnicas) solo necesitan `class="page-hero"` — el degradado se ve solo. Páginas CON foto agregan, como primer hijo del `<section>`, `<div class="page-hero-bg" aria-hidden="true"><img src="..." alt="" loading="eager" fetchpriority="high" decoding="async"></div>` — el `::after` de `.page-hero-bg` reutiliza el mismo `--ph-gradient`, así que la foto y el degradado siempre coinciden.
  - El degradado (`--ph-gradient` en `article.css`, y el de `.qs-hero-media::after` en `quienes-somos.css`) usa `--navy`/`--teal`/`--teal-light` (que, tras la migración de paleta de arriba, ya valen azul oscuro/petróleo/cielo — 100% oficiales). Si agregas un hero nuevo, reutiliza esos mismos tokens en vez de inventar una paleta local — ya pasó 4 veces antes de esta migración (`--route-*` en `base.css`, `--pb-*` en `productos.css`, `--ib-*` en `quienes-somos.css`, y los rgba() sueltos en media docena de hojas de estilo más).
- **Texto justificado (2026-09-21):** toda la prosa de lectura corrida del sitio va `text-align:justify`. La lista de selectores vive en **UN solo bloque al final de `assets/css/base.css`** ("JUSTIFICADO DE PROSA"), no repartida por hoja — si justificas algo nuevo, agrégalo ahí, no en la hoja de la página. Siempre acompañado de `hyphens:auto` (+ `-webkit-hyphens`) y `hyphenate-limit-chars:6 3 3`: sin partición de palabras el español justificado abre "ríos" de espacio en blanco. Depende del `lang` del `<html>`, que `i18n.js` ya reescribe al cambiar de idioma.
  - **Edge headless NO tiene los diccionarios de partición** (los baja Chrome por el actualizador de componentes y un perfil nuevo nunca los tiene), ni en ES ni en EN. En las capturas las columnas estrechas salen con más hueco entre palabras del que ve el usuario real: es un artefacto del entorno, como el `.reveal` a medio fundir. No lo "arregles" quitando el justificado.
  - **Qué NO se justifica, a propósito:** texto centrado (`.section-head`, `.stat-card`, `.pq-head`), micro-copy de tarjetas de menos de ~40 caracteres por línea (`.icon-item`, `.process-step`, `.ra-plan`, `.sgi-pillar`, `.pq-card`, `.product-desc`…) —con una excepción: las tarjetas de `compromiso-social` (`.cs-face`, `.cs-pillar`, `.cs-card`) sí se justifican aunque `.cs-pillar` ronde los 34 caracteres, porque en bandera la rejilla de cuatro se veía deshilachada; se compensa con `hyphenate-limit-chars:5 2 2` en su propia hoja—, citas (`blockquote`, `.qs-quote`), los heros grandes de `index`/`productos`/`quienes-somos` (tipografía de presentación, y `.hero-inner` pasa a `text-align:center` bajo 980 px) y el texto de formularios, tablas y navegación. Los heros de página interior (`.page-hero p`) sí van justificados: son texto de cuerpo.
- **Medida de lectura en `rem`, no en `ch` (2026-09-21):** `.ra-block`, `.sgi-block`, `.pd-block` y `.article-grid--full > .prose > p` acotan la prosa a **`max-width:54rem`** (≈864 px ≈ 74 caracteres). Antes eran `74ch`, pero `ch` se mide sobre la letra del **propio** elemento, así que el lead (1.06rem) salía 70 px más ancho que el cuerpo (.98rem) y, al justificar, los dos márgenes derechos dejaban de coincidir. No lo devuelvas a `ch`. La regla de `article.css` se agregó porque `compromiso-social` escribe `<p>` directo dentro de `.prose` y se quedaba con los 1.136 px del contenedor (~115 caracteres por línea).
- **Navbar y footer estandarizados (2026-09-17):** las 27 páginas con navbar comparten un navbar y un footer **byte a byte idénticos**, salvo dos cosas que SÍ cambian por página: el prefijo de profundidad (`../` en subcarpetas, nada en la raíz) y el `aria-current="page"` del enlace de la página actual (solo en `index`, `productos`, `quienes-somos` y `contacto`). Antes cada página traía su propia variante: unas ofrecían PQRSF y otras no, la columna "Enlaces" tenía 3 juegos de enlaces distintos y `quienes-somos` se había quedado sin "Productos" en el menú móvil.
  - Los cinco bloques compartidos son `.navbar-top`, `.drawer-top`, `.drawer-footer`, `.footer-links-col` y `.container.footer-bottom`. Si tocas uno, **replícalo en las 27** — se comprueba comparando los bloques de todas las páginas contra los de `index.html` tras normalizar `../` y `aria-current`.
  - Lo ÚNICO que es propio de cada página es el `<div class="navbar-route">` (y su gemelo móvil `<nav class="drawer-route">`): son la navegación por secciones de esa página concreta. Su `aria-label` es `"Navegación de esta página"` / `"…(móvil)"` en todas menos `index.html`, que al ser la navegación principal del sitio conserva `"Navegación principal"` / `"Navegación principal móvil"`.
  - La columna "Enlaces" del footer lleva un subgrupo **Sostenibilidad** (`<h4 class="footer-sub">`, estilo en `footer.css`) con SGI / Compromiso social / Ambiente. Se agregó porque esas tres páginas solo se alcanzaban desde `index.html`: desde una ficha técnica no había forma de llegar. La barra inferior lleva el lema, Política de protección de datos y PQRSF.
  - **Las etiquetas de esos tres enlaces van abreviadas a propósito** (decisión de dirección): el título completo de la página, no. "Sistema de Gestión Integrado" → **SGI** (`IMS` en EN) y "Responsabilidad ambiental" → **Ambiente** (`Environment` en EN). Con el nombre largo la columna partía en dos líneas cada enlace y estiraba el pie sin necesidad. Si agregas otro enlace a ese subgrupo, mantenlo en una sola línea.
- El enlace "Productos" del navbar/footer/breadcrumbs en TODAS las páginas apunta a `productos.html` (catálogo completo, en la raíz del sitio) — ya no a `index.html#productos`. El index conserva una sección `#productos` recortada (solo algunos productos destacados) con un botón "Ver todo el portafolio" hacia `productos.html`.
- Las tarjetas de producto (`.product-card` / `.products-grid`) muestran una imagen estática y, si existe, un video que se revela en hover/focus (`assets/js/home.js`). Hay DOS diseños con los mismos nombres de clase, a propósito, para compartir esa lógica de video: el del index (`assets/css/home.css`) y el del catálogo (`assets/css/productos.css`, que no carga home.css). Productos "en desarrollo" (sin foto ni video reales) usan `assets/img/en-desarrollo.png` como cover fijo — es la solución intencional para que no se vean en blanco, no un placeholder pendiente de arreglar.
- `productos.html` sigue usando sus propios tokens `--pb-*` (definidos en `.prod-page`, `assets/css/productos.css`) por legibilidad semántica del código, pero desde la migración de paleta son simples alias de `var(--brand-*)` — ya no una paleta aparte. Sigue sin usar `.btn-primary`/`.btn-outline` de `base.css` ahí (son componentes con su propio estilo, pensados para el resto del sitio) — usa los `--pb-*` locales.
- **Contacto (2026-09-15):** el botón "Contacto" de TODAS las páginas apunta a `contacto.html` (raíz; `../contacto.html` desde subcarpetas), ya no al footer `#contacto`. Es un formulario sin backend (`assets/css/contacto.css` + `assets/js/contacto.js`): valida, arma el mensaje y abre `wa.me/573113337046` con el texto ya escrito (el usuario debe presionar Enviar en WhatsApp — no se puede automatizar), con alternativa `mailto:contacto@petroilsa.com`. Acepta `?producto=<código>` (ej. `P-50%2F10`) para preseleccionar el producto; los `value` de los checkboxes son exactamente los códigos de producto. La casilla de aceptación enlaza a `legal/politica-datos.html` (2026-09-20); antes era un `href="#" data-pending-link` y el manejador que le quitaba el salto ya se borró de `contacto.js`.
- **PQRSF (2026-09-16):** `legal/pqrsf.html` + `assets/css/pqrsf.css` + `assets/js/pqrsf.js`, mismo patrón sin backend que Contacto pero con destino ÚNICO **`pqrsf@petroilsa.com`** y **sin WhatsApp ni teléfono** (decisión de dirección — no los agregues). Formulario en 4 pasos con "ticket" lateral en vivo; referencia `PQRSF-<letra>-AAMMDD-NNNN`; canales Gmail / Outlook.com / Outlook 365 / mailto / copiar. La casilla de la política de datos es obligatoria (enlaza a `politica-datos.html`) y la aceptación queda escrita en el cuerpo del correo. Cada tipo tiene un color de marca vía `.pq-c--p/q/r/s/f` (`--c`/`--c-ink`). Acepta `?tipo=reclamo#radicar` (sin tildes). Plazos: 15 días hábiles peticiones/quejas/reclamos, 8 sugerencias/felicitaciones — si cambian, actualiza `TYPES` en el JS, el HTML, `pqrsf.json` y la entrada `pqrsf` de `aira-kb.js`. El `.toast` que usa `pqrsf.js` vive en `pqrsf.css` (antes estaba en `chatbot.css`, que ya no existe).
- **Compromiso social — reescrita desde el libreto oficial (2026-09-21):** `sostenibilidad/compromiso-social.html` no es página de ejemplo. **Todo su texto sale de `LIBRETO VIDEO PETROIL.docx`** (el guion del video corporativo, en `Para construccion/Compromiso social/`), incluidas las cuatro cifras: **+8 patentes**, **−30 % de gases de efecto invernadero**, **>15 veces menos MP10 y MP2.5 que los parámetros permisibles** y **hasta −8,75 % de consumo** por eficiencia energética. No hay ni una cifra inventada, y no se agregan indicadores nuevos sin fuente escrita.
  - **Es la única página de `sostenibilidad/` que NO usa `.article-wrap`/`.prose`.** Sus hermanas `sgi` y `responsabilidad-ambiental` son una columna de prosa; esta sigue el lenguaje de `nosotros/quienes-somos.html`: `<section class="cs-sec">` a todo el ancho que alternan blanco (`--plain`) y `--mist`, cada una con cabecera centrada `.cs-head` (680 px) y rejillas de tarjetas, más una franja `.cs-cta` oscura al final. Estilos en **`assets/css/compromiso-social.css`** (`.cs-*`), cargada DESPUÉS de `article.css` —de ahí salen `.page-hero`, `--ph-gradient` y `.photo-gallery`—. Se hizo así el 2026-09-21 porque la versión anterior, de prosa corrida, se veía "básica y demasiado ancha".
  - Los paneles oscuros (`.cs-face--dark`, `.cs-principle`, `.cs-results`, `.cs-cta`) repiten el patrón de `.ra-criteria`: `background-color` OPACO + `background-image:var(--ph-gradient)`. El degradado es semitransparente; sin el color de base el panel sale lavado. Llevan burbujeo con `<div class="hero-particles" data-particles="N">` y `--rise:420px` (los paneles son bajos); el contenido va en `z-index:2` para que las burbujas nunca lo tapen.
  - **No lleva `.reveal`**, igual que `responsabilidad-ambiental`: el contenido nunca depende del IntersectionObserver.
  - La página tiene **dos mitades a propósito**, porque así lo plantea el propio libreto: *"las mejores prácticas empresariales nos han llevado no solo a mejorar nuestros productos y servicios, sino también a impulsar el desarrollo social"*. Esa idea es literalmente la sección 1 (las dos tarjetas `.cs-face` 01/02). Luego `#nuestra-gente` desarrolla el compromiso con los colaboradores y sus familias (4 `.cs-pillar` + panel `.cs-results`), y `#entorno` el ambiental del libreto (4 `.cs-figure` + 4 `.cs-card`: FISTech®, contaminantes, Mecanismos de Desarrollo Limpio e hidrógeno verde). **`#entorno` NO duplica a `responsabilidad-ambiental.html`**: esa página trata los instrumentos de gestión (plan de manejo, licencia, ISO 14001) y esta el desempeño del producto; se enlazan entre sí con `.cs-links`, mantén esa división.
  - **Ojo con el reclamo de "primera refinería":** el libreto distingue dos cosas que el resto del sitio mezcla — primera de **América Latina** en combustibles *amigables con el medio ambiente*, y primera del **mundo** en combustibles *de transición* (FISTech®). En `compromiso-social` y en las entradas `transicion`/`diferencial` de AIRA ya está separado así; el `<meta name="description">` de `index.html` y compañía todavía dice solo "primera refinería en América Latina en producir combustibles de transición". Pendiente de decisión de dirección antes de propagarlo.
  - Fotos reales en `assets/img/compromiso-social/` (`Background.jpeg` → hero `hero-mujeres-operacion.jpg`; las otras tres, a la galería, que sigue usando `.photo-gallery` + `gallery.js` con el recorte subido a 300 px solo dentro de `.cs-sec`).
  - Del bloque "JUSTIFICADO DE PROSA" de `base.css` esta página solo aporta **`.cs-face p`, `.cs-pillar p` y `.cs-card p`**: las tres familias de tarjetas con texto a la izquierda. Lo centrado (cabeceras `.cs-head`, franja del principio, resultados, cifras, nota y CTA) no se puede justificar, así que se reparte con `text-wrap:balance` / `pretty` —definido en `compromiso-social.css`— para que ningún renglón quede con una sola palabra. Para que `balance` surta efecto el elemento tiene que ser de bloque: por eso `.cs-result span` lleva `display:block`.
- **Responsabilidad ambiental — contenido real (2026-09-18):** `sostenibilidad/responsabilidad-ambiental.html` **ya no es página de ejemplo**: fuera el `.example-banner`, el `.disclaimer-full` y el `[Ejemplo]` del `<title>`. El lead del hero y el párrafo de "Nuestro enfoque ambiental" son los textos oficiales que entregó la empresa, y la frase del `blockquote` la fijó dirección ("Mejorar el aire que respiramos implica controlar, monitorear y gestionar responsablemente los procesos que forman parte de nuestra operación"). Lo que antes eran "Ejes de trabajo" (monitoreo de emisiones, vertimientos, residuos, combustibles limpios — inventados) son ahora los cuatro **instrumentos de gestión que nombró la empresa**: Plan de Manejo Ambiental, Plan de Seguimiento y Monitoreo, Plan de Contingencias y Licencia Ambiental. **Se eliminó el gráfico de barras de "tendencia de emisiones"**: sus cifras (2022=100, −40 %…) eran de ejemplo. La página NO lleva indicadores a propósito — si dirección entrega cifras reales van en una sección nueva; no se inventan. La certificación ISO sí se mantiene: es un dato real que ya afirman `index.html`, `quienes-somos` y `sgi`.
  - **Se quitó la columna `.toc`** (era la misma navegación del navbar inferior, duplicada) → la página usa `article-grid--full`, igual que `compromiso-social`. Como la prosa pasa a ocupar todo el ancho, `.ra-block > p` limita la medida de lectura a 74ch; los bloques visuales sí ocupan el ancho completo.
  - Estilos propios en **`assets/css/responsabilidad-ambiental.css`** (`.ra-*`), cargado después de `article.css`. Sus dos paneles oscuros reutilizan el fondo del hero (`background-color:var(--brand-navy)` + `background-image:var(--ph-gradient)`); el color base opaco es obligatorio porque `--ph-gradient` es semitransparente y sin él el panel sale lavado sobre el blanco de la prosa. Las tarjetas de instrumento usan el par `--c` (acento, puede ser claro) / `--c-ink` (versión legible para texto e icono), mismo patrón que `.pq-c--*` de `pqrsf.css`.
  - **No lleva `.reveal`**, igual que `compromiso-social`: son páginas cortas y así el contenido nunca depende del IntersectionObserver.
- **SGI — contenido real (2026-09-18):** `sostenibilidad/sgi.html` **ya no es página de ejemplo**: fuera el `.example-banner`, el `.disclaimer-full` y el `[Ejemplo]` del `<title>`. Se rehízo con el material oficial de `Para construccion/SGI` y con la estructura de su hermana `responsabilidad-ambiental` (sin columna `.toc`, `.article-grid--full`, secciones `<section class="sgi-block" id>` y hoja propia `assets/css/sgi.css` cargada DESPUÉS de `article.css`). Todo dato de la página sale de un documento firmado:
  - **Los tres pilares van en el orden 9001 → 14001 → 45001** (lo pidió dirección) y cada tarjeta cita su certificado real: CO24.08378 / CO24.08379 / CO24.08380, emitidos por BVQI Colombia Ltda. (Bureau Veritas), ciclo iniciado el 20/11/2024 y vigentes hasta el 19/11/2027. Se eliminó la cuarta caja "Mejora continua": duplicaba la sección del ciclo PHVA que va justo debajo.
  - **Los indicadores son cifras de la empresa, con el nombre en positivo a propósito:** 1.103 días consecutivos de operación sin accidentes · 0 incidentes ambientales ("Operación libre de…") · 5,5 de cultura en seguridad (etapa independiente) · 1 evento de seguridad de proceso ("Único evento… registrado"). Las cifras NO se tocan; si cambian, actualiza también la entrada `sgi` de `aira-kb.js`.
  - **"Normas para visitantes a la Refinería"** transcribe la cartilla `SG-SGI-GUI-005` a HTML real (4 tarjetas `.sgi-rule`: instrucciones de SST, EPP mínimos, documentos de ingreso y prohibiciones) en vez de servir solo la lámina: así es texto buscable, traducible y legible en móvil. Las prohibiciones usan `var(--warn)`, el naranja reservado a avisos.
  - **De la cartilla se sirve la IMAGEN, no el PDF** (la fuente traía los dos; usar ambos confunde). `assets/img/sgi/cartilla-visitantes.webp` es la lámina completa —1.483 × 3.840 px— y se abre en pestaña nueva desde la tarjeta `.sgi-cartilla`, que muestra el recorte `cartilla-visitantes-preview.webp`. **No la metas en el visor de `gallery.js`**: con `max-height:100%` una lámina tan alta cabría entera en pantalla pero con el texto ilegible.
  - La infografía de propósitos (`assets/img/sgi/propositos-sgi.webp`, de `sgi.png`) SÍ usa el visor: es la única `.photo-gallery` de la página, así que `gallery.js` la dibuja sin flechas ni miniaturas. `sgi.css` anula ahí la rejilla de 3 columnas y el recorte a 230 px de alto.
  - Los `.webp` se generaron con **ffmpeg** (no hay ImageMagick ni Python en este equipo): `ffmpeg -i <origen> -vf scale=<ancho>:-2 -c:v libwebp -quality 86 -preset picture <destino>`.
  - El nombre oficial en los documentos firmados es **"Sistema de Gestión Integrado"** (masculino). El 2026-09-18 se renombró en todo el sitio: `index.html`, `index.json`, `responsabilidad-ambiental` (.html/.json) y `aira-kb.js`. No lo vuelvas a escribir "Integrada".
- **Política de datos — contenido real (2026-09-20):** `legal/politica-datos.html` **ya no es página de ejemplo**: fuera el `.example-banner`, el `.disclaimer-full` y el `[Ejemplo]` del `<title>`. Se rehízo con el documento oficial **SG-GG-POL-004, versión 2** (actualización integral del 15/09/2026, firmada por el Chairman) y con la estructura de sus hermanas `sgi`/`responsabilidad-ambiental`: sin columna `.toc`, `.article-grid--full`, secciones `<section class="pd-block" id>` y hoja propia `assets/css/politica-datos.css` cargada DESPUÉS de `article.css`. Todo dato de la página sale del documento firmado.
  - **El responsable NO es "Petroil S.A.": es `PETROIL CAPITAL HOLDINGS S.A.S.`, NIT 900.985.911-6**, y el canal de habeas data es `contacto@petroilsa.com` + **WhatsApp 320 284 3422** (distinto del 311 333 7046 comercial que usa el footer). Esos datos salen de la tabla de la página 2 del PDF; no los homogenices con los del resto del sitio.
  - El encabezado de cada página del PDF dice "Versión: 1 – Fecha: 26/07/2023": quedó sin actualizar. La versión buena es la del **control de cambios de la portada** (versión 2, 15/09/2026), que es la que cita el sitio.
  - El PDF vive en `assets/docs/legal/` y se ofrece con **dos acciones sobre el mismo archivo**: `target="_blank"` (ver en pestaña nueva) y `download` (guardar). La página es un **resumen** — el `.pd-notice` de arriba lo dice y remite al PDF, que es el texto que rige.
  - La página NO inventa plazos de respuesta: el documento solo dice "dentro de los términos legales", así que la nota del panel de canales remite a `pqrsf.html#radicar`, donde sí están los 15 / 8 días hábiles.
  - El aviso de datos sensibles, menores y videovigilancia usa `var(--warn)` — es una precaución para el titular, no una finalidad más.
- **`assets/docs/` vuelve, SOLO para documentos oficiales firmados (2026-09-18 / 2026-09-20):** `assets/docs/sgi/` guarda los 6 PDF oficiales que enlaza la sección "Documentos del sistema" (política, los 3 certificados ISO, reglamento de higiene y reglas vitales) y `assets/docs/legal/` el de la política de tratamiento de datos. Van **byte a byte como los entregó la empresa** —son documentos controlados y firmados, no se recomprimen— por eso el reglamento pesa 11,5 MB; cada tarjeta `.doc-card` / `.pd-doc` avisa del formato y el peso. Esto NO reabre las fichas técnicas en PDF (ver el punto siguiente): la excepción es para documentación controlada, no para material comercial.
- **Sin fichas técnicas descargables:** por decisión de dirección las fichas NO se descargan en PDF (se retiró `assets/docs/` en su momento; hoy solo existen `assets/docs/sgi/` y `assets/docs/legal/`, ver el punto anterior). En `fichas-tecnicas/` el CTA principal es "Solicitar cotización" → `../contacto.html?producto=<código>#formulario` (hero, tarjeta `.pds-side-card.quote` y franja `.pds-quote`). No vuelvas a agregar botones de descarga.
- **Fichas "comerciales" con ave emblema (2026-09-16):** por decisión de dirección las fichas ya no muestran tabla técnica ni NFPA, sino Descripción / Beneficios principales / Aplicaciones / Características destacadas + bloque `.pds-emblem` (ave de la "Selección Biodiversa"). Ya migradas: 50-10, 250, 800-hc, 800-g, 90, 500, 230-ms, 100-mifos, 300 (contenido de `productos/DESCRIPCIÓN DETALLADA DE PRODUCTOS.docx`). Las otras 6 siguen con el formato viejo hasta que haya información confirmada. Textos con claves `pd.*` en el JSON de cada ficha; el navbar inferior apunta a `#descripcion #beneficios #aplicaciones #caracteristicas #ave-emblema #contacto` (etiquetas `nav.*` en `common.json`). El hero usa `.pds-hero--bird` con un recorte propio `assets/img/aves/hero-<ave>.jpg` (1400×1120, cabeza del ave arriba y a la derecha del centro — no ajustes `object-position` por ave, recorta la foto); `.pds-hero--light` para fotos de fondo claro. El bloque del ave usa la foto completa `assets/img/aves/<ave>.jpg` (1200 px).
- **"Otros combustibles Petroil"** en las fichas usa clases propias `.pds-rel-*` (en `ficha-tecnica.css`). NO uses `.products-grid`/`.product-card` ahí: sus estilos viven en `home.css`/`productos.css`, que las fichas no cargan (así se rompió antes). Criterio de las 4 recomendadas: misma familia (reales) → otras familias (reales) → "en desarrollo".
- `productos.js` acepta filtros por URL: `productos.html?cat=fuel-oils#catalogo` y `?sector=maritimo#catalogo` (valores = `data-cat` de las pestañas / `value` del selector de sector).
- **Portal de clientes** está comentado (`<!-- ... -->`) en navbar, drawer y footer de todas las páginas hasta que salga a producción. Para reactivarlo, descomenta esos enlaces; no borres `portal-clientes/`.
- Recursos: nunca enlaces imágenes/favicons de `petroilsa.com/wp-content/...` (el WordPress anterior; responde 404). Todo va local en `assets/img/`. Tampoco enlaces URLs del WordPress viejo (`petroilsa.com/servicios/`, `/noticias/`).
- **`.reveal` y elementos `position:fixed` (2026-09-17):** nunca pongas un modal/overlay fijo DENTRO de una sección `.reveal`: cualquier `transform` en un ancestro hace que el `fixed` se posicione contra ese ancestro y no contra la pantalla. Así se rompieron los perfiles del equipo en `nosotros/quienes-somos.html` (el modal se abría fuera de vista) — el modal ahora va fuera de la sección y `.reveal.in` usa `transform:none`. `nav.js` revela también secciones más altas que la pantalla (antes, con solo `threshold:.15`, "Equipo" nunca aparecía en celular).
- **Galería de fotos con visor (2026-09-17):** `assets/js/gallery.js` convierte cualquier `.photo-gallery` de la página en una galería ampliable (flechas ← →, miniaturas, deslizar en móvil, Esc para cerrar, foco atrapado y devuelto a la foto de origen). Es mejora progresiva: sin el script la rejilla se ve igual, solo que no amplía — por eso **no hay marcado del visor en el HTML**, lo construye el JS y lo cuelga de `<body>` (nunca dentro de la sección: un `transform` de un ancestro rompería el `position:fixed`, ver el punto anterior). El CSS vive en `article.css` junto a `.photo-gallery`, así que la página tiene que cargar esa hoja. Para añadirlo a una página nueva basta con enlazar `gallery.js`. Cada `<img>` admite `data-full` (versión mayor que la de la rejilla) y `data-cap-key` (clave i18n del pie de foto; si falta, usa el `alt`). Lo usan `sostenibilidad/compromiso-social.html` y la nota de la visita de la embajada. Sus aria-labels son las claves `gallery.*` de `common.json`.
- **Burbujeo (partículas) fuera del hero:** `<div class="hero-particles qs-particles" data-particles="N" aria-hidden="true">` dentro de un padre `position:relative; overflow:hidden` — `hero-particles.js` rellena cualquier `[data-particles]` además de `#heroParticles`. La altura de subida se ajusta con `--rise` sobre `.particle` (620px por defecto). Ejemplos en `quienes-somos` (tarjeta Propósito, Ruta 2031, Chairman, CTA).
- Edge headless tiene `prefers-reduced-motion: reduce` y no ejecuta el IntersectionObserver: en las capturas `.reveal` se ve siempre y `.qs-particles` se oculta; no lo confundas con un bug.
  - **Ojo: eso vale para `--headless` clásico, NO siempre para `--headless=new`.** Con `--headless=new` sí corre el observador y sí anima: las secciones `.reveal` quedan capturadas a medio fundido y la página entera sale "lavada" (texto gris claro, paneles oscuros deslavados), sobre todo con ventanas altas, donde varias secciones entran a la vez. `--virtual-time-budget` no lo arregla (tampoco con 12 s). Si una captura sale pálida, compárala con una página sin `.reveal` (`compromiso-social`) antes de tocar ningún color: casi siempre es esto y no un problema de contraste. Otro artefacto de `--headless=new` con ventanas más altas que la página: los elementos `position:fixed` (píldora de idioma del navbar, teaser de AIRA) se repintan también abajo, pegados al pie — tampoco es un bug.
- Todas las páginas con navbar usan `<div class="scroll-track"><div class="scroll-progress" id="scrollProgress"></div></div>` (sin el contenedor la barra móvil no se ve) y el chat AIRA, igual que index.html.

## Chat AIRA (migrado a todo el sitio el 2026-09-16)

**Todas las páginas usan el mismo chat** (`assets/css/aira.css` + `assets/js/aira-kb.js` + `assets/js/aira.js`, bloque `<div id="aira">`, clases `.aira-*` dentro de `#aira`). Ya no hay dos maquetas: la vieja (`chatbot.css`/`chatbot.js`, clases `.chat-*`) **se borró del repo el 2026-09-17**, porque su última usuaria era `nosotros/ala-de-sable.html` y dirección la retiró de la versión final. Su única regla todavía viva, `.toast` (la usa `pqrsf.js`), se mudó al final de `pqrsf.css`. Si te topas con una clase `.chat-*`, es código muerto.

Si agregas una página nueva, copia el bloque `<div id="aira">` de cualquier página migrada, pon `data-base="../"` (o `""` en la raíz — prefijo de los enlaces de la base de conocimiento) y ajusta las rutas de `<img>`/`<link>`/`<script>` según la profundidad de carpetas.

**Sin API, sin IA, sin backend** — por decisión de dirección. El chat responde por coincidencia de palabras clave, todo en el navegador.

- **Para enseñarle algo nuevo a AIRA se edita SOLO `assets/js/aira-kb.js`.** Nunca hace falta tocar `aira.js`. El formato de una entrada (`p` = frases de peso alto, `k` = palabras sueltas sin tilde, `a` = respuesta HTML, `links`, `next`) está documentado en la cabecera de ese archivo.
- La base va en **`.js` y no en `.json` a propósito**: un `fetch()` de JSON falla bajo `file://` por CORS, y este proyecto se revisa abriendo las páginas con Edge headless sobre `file:///`. Si la conviertes a JSON, el chat deja de funcionar en ese flujo.
- El motor normaliza la pregunta (minúsculas, sin tildes ni signos), quita palabras vacías, expande sinónimos y **corrige erratas** contra el vocabulario de la propia base. Por eso las keywords se escriben SIN tilde: `diesel` ya matchea `diésel`, `DIÉSEL` y `Diesel`.
- **Si cambias una ficha técnica, actualiza su entrada `p-<código>` en `aira-kb.js`** (y las comparativas/guías que citen esa cifra: `azufre`, `poder-calorifico`, `comparar-*`, `aves-emblema`). El 2026-09-16 la base tenía cifras de las fichas viejas (p. ej. 800 HC "-45 % / 14.579 BTU/lb") que ya no coincidían con el sitio.
- **AIRA no cita "datos de ejemplo" como reales.** Las fichas sin formato nuevo (40 A MAX, 70, 87 R, RF-110+, RF-100+ y la densidad de la 60) siguen rotuladas como ejemplo: la base solo dice que la ficha oficial está pendiente. Ya no queda ninguna PÁGINA de ejemplo: la política de datos se reescribió con el documento oficial el 2026-09-20 y su entrada `datos-personales` cita razón social, NIT, correo de habeas data y los derechos del titular, y enlaza el PDF. Las tres páginas de sostenibilidad que ya tienen contenido oficial también se citan como reales — `sgi` se reescribió el 2026-09-18 (y es la única que publica cifras: indicadores, números de certificado y normas para visitantes, con la entrada nueva `visitantes`), `comunidad` se reescribió el 2026-09-17 y `ambiental` el 2026-09-18 (ahora responde con los criterios prevención/control/mitigación y los cuatro instrumentos; se le quitaron el "monitoreo permanente de emisiones", el "reducir año a año la huella de carbono" y la mención a la ANLA, que venían del gráfico y los textos de ejemplo que ya no existen en la página).
- **Los sinónimos de `aira-kb.js` solo funcionan con UNA palabra (2026-09-21).** `SYN` se construye normalizando cada valor entero y luego se consulta **token a token**, así que un valor con espacios (`'mecanismo de desarrollo limpio'`, `'bonos de carbono'`) nunca coincide con nada: es peso muerto que parece estar funcionando. Para una expresión de varias palabras, mete la frase en la `p` de la entrada, no en `synonyms`. Y ojo con los puntos: `normalize()` los convierte en espacio, así que "PM2.5" llega como `pm2 5` y "PM 2.5" como `pm 2 5` — son dos frases distintas y hay que declarar ambas (ya están, junto con las variantes `mp*` del orden en español, en la entrada `contaminantes`).
- **Siglas de menos de 5 letras necesitan sinónimos explícitos**: el corrector de erratas no las toca. "PQRS" no respondía porque solo existía "pqrsf"; ahora `synonyms.pqrsf` incluye pqrs/pqr/pqrsd.
- **Charla / humanización (2026-09-21):** además de `entries`, `aira-kb.js` tiene un bloque **`smalltalk`** — 18 reglas de conversación (saludos, cortesía, “¿puedes hacer algo por mí?”, elogios, reclamos, “¿qué hora es?”). **No lleva ni un dato de Petroil**: es solo la forma de contestar mientras se llega al dato, por eso la base no engorda ni se vuelve lenta. Antes de esto, “¿puedes ayudarme?” caía en “no encontré información sobre…”, que es justo lo que delata a un buscador disfrazado de chat. Cada regla es `{id, re, a, chips}`; el `re` es una **cadena** (se compila una sola vez al cargar) que se prueba contra la pregunta ya normalizada, así que **se escribe sin tildes**, igual que las keywords. Si `a` es un **array**, AIRA elige una variante al azar evitando la anterior: contestar siempre con las mismas palabras exactas es lo primero que delata a la máquina. Admite los marcadores `{saludo}` (buenos días / tardes / noches según la hora real del visitante), `{hora}`, `{fecha}` y `{tema}`.
  - **Lo importante son los guardas, no las frases.** Por defecto una regla solo gana si la base NO está segura (puntaje < `CONFIDENT`) — así “hola, ¿qué es una PQRS?” sigue respondiendo PQRSF y no un saludo. Con **`weak: true`** solo gana si la base no reconoció nada (< `MAYBE`): es obligatorio en las reglas de palabras amplias (`puedes`, `ayudame`, `ok`, `que opinas`), porque si no “¿me puedes dar el precio del 50/10?” contestaría “claro, dime qué necesitas” en vez del precio. Con **`force: true`** gana siempre, y eso solo se usa donde no puede haber otra lectura — hoy únicamente `nombre`, porque “¿cómo te llamas?” puntuaba 91 en `contacto`: el corrector de erratas convierte “llamas” en “llamar”, declarado como sinónimo de “contactar”. **`max: N`** limita la regla a mensajes de N palabras o menos, y **`ctx: true`** la hace depender de la respuesta anterior (`{tema}`, más sus chips y enlaces) — es lo que hace funcionar un “cuéntame más” a secas.
  - Las respuestas de tema también se humanizaron un poco: si la pregunta viene envuelta en cortesía (“¿me podrías decir…?”) **y** la base responde con certeza, se abre con un “Claro 🙂” variable; la frase de “creo que preguntas por esto” y el “no encontré nada” rotan entre varias versiones; y cuando no hay coincidencia pero la base rozó algo, se proponen **esos** temas en vez de la lista fija de siempre.
  - Para comprobar que una regla nueva no se está comiendo preguntas reales: **`AIRA.probe('tu pregunta')`** en la consola devuelve `{chat, top, results}`. Si `chat` trae un id, esa regla responde y el tema de `results[0]` ni se usa.
- **Quién construyó el sitio:** las cuatro entradas `creador`, `creador-architect`, `creador-perfil` y `creador-contacto` (`cat: 'sistema'`, fuera del menú de temas) responden por el autor del sitio — Edwin Belisario Calderón Aguilera, «The Architect» — y están escritas **de menos a más a propósito**: la primera contesta lo justo y las otras tres (apodo, perfil, contacto) solo aparecen si la persona sigue indagando o pulsa un chip. Se publica **solo su correo**, nunca su teléfono. Si tocas alguna, mantén esa progresión y el tono: el sitio es de Petroil y esto no compite con el contenido de la empresa.
- **AIRA no lee los PDF.** Lo que sabe de los documentos descargables (los 6 de `assets/docs/sgi/` y la política de datos) es lo que las páginas del sitio resumen de ellos y alguien transcribió a `aira-kb.js`: códigos (`SG-SGI-POL-001`, `SG-GG-POL-004`), números de certificado, responsable y NIT. El texto de los PDF en sí **no está indexado** — si preguntan qué dice el Reglamento de Higiene, AIRA no lo sabe. Es consecuente con la regla de que todo dato salga de una página publicada: para que responda algo de un PDF, primero tiene que estar escrito en una página.
- Las entradas con una `cat` que no está en `meta.categories` (hoy `lideres` y `sistema`) se buscan igual pero no aparecen en el menú de temas.
- Umbrales en `aira.js`: puntaje ≥50 responde directo, 16–49 responde ofreciendo alternativas, <16 admite que no sabe y ofrece un asesor. Al agregar entradas es fácil que dos se roben las mismas palabras — **compruébalo con `AIRA.match('tu pregunta')` en la consola**, que devuelve el ranking con puntajes (o `AIRA.probe()`, que además dice si contesta la capa de charla).
- **Listas de sugerencias (2026-09-17):** las burbujas con botones de tema (categoría, alternativas, "no entendí") se pintan con `suggestList()` en `aira.js`, que muestra `SUGGEST_VISIBLE` (4) y pliega el resto tras un "Ver N más". No vuelvas a escribir el `<div class="aira-suggest">` a mano: el estado plegado viaja en el atributo `hidden` del HTML, así que `save()` lo persiste solo y `wireSuggestions()` recablea el botón al restaurar.
- **Anclaje del scroll (2026-09-17):** al responder, el chat NO baja al final; `focusAnswer(row)` deja la primera línea de la respuesta arriba (`row.offsetTop − 12`, acotado con `Math.min` al fondo real) para que se lea de arriba abajo. De ahí que `.aira-log` sea `position:relative` — es el `offsetParent` que hace válido ese `offsetTop`. `addUser`/`showTyping` sí siguen bajando del todo.
- Deep link: `href="#chat"` abre el chat y `href="#chat=quiero cotizar"` lo abre con la pregunta ya hecha. El token es `chat` y NO `aira` porque el contenedor tiene `id="aira"` y el navegador saltaría hasta el widget antes de que corra el JS.
- **Las respuestas solo existen en español.** `common.json` traduce el marco del chat (`aira.*`), pero el campo `a` de cada entrada es texto plano en ES. Para traducirlo habría que volver `a`/`title` objetos por idioma y leer el idioma activo con `PetroilI18n.t`.
- La conversación se guarda en `sessionStorage` (no `localStorage`): sobrevive a navegar entre páginas, pero al cerrar la pestaña empieza de cero.
- `scripts/devserver.ps1` soporta peticiones por rango (206) — sin eso los videos salían como "(canceled)" en el inspector al servir en local.
- Ojo con las fotos: `Petroil_fotosplanta_46/48/60.jpg` son fauna (loros, un pájaro, una iguana), no planta industrial. Las fotos industriales reales utilizables como fondo son `Petroil_fotosplanta_29.jpg`, `noticias/euro-vi-petroil-50-10-refineria.jpg`, `noticias/euro6-refineria-petroil.jpg` y `noticias/embajada-eeuu-recorrido-planta.jpg`. Las `quienes-somos/ruta-*.png` son miniaturas (~230px), no sirven de fondo.
